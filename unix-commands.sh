# COPY
cp /source /dest
# Copy an entire directory from /source folder to /dest folder
cp -a /source/. /dest/

# MOVE
mv /source /dest
# Move multiple directories to a new directory
mv dir1 dir2 /path/to/dest/
